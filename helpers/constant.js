//time to expired access token token: 30 minute
const TTL_ACCESS_TOKEN = 3 * 60;
//time to expired refresh token token: 1 year
const TTL_REFRESH_TOKEN = "1y";

module.exports = {
  TTL_ACCESS_TOKEN,
  TTL_REFRESH_TOKEN,
};
